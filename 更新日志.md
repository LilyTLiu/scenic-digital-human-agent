# 更新日志

## 一、真人数字人（OpenTalking + QuickTalk）

- 集成 OpenTalking 开源数字人引擎，RTX 4060 GPU 实时驱动
- WebRTC 视频流直连，延迟 < 1 秒
- 支持 TTS 音频驱动口型同步，字幕与语音逐字匹配
- 全屏语音对话模式：底部麦克风按钮 + 字幕滚动 + 4 角色切换
- 对话页浮动视频窗：打字提问 → AI 回复文字 → 数字人自动说话

**4 导游映射：**
| 导游 | 数字人形象 | 声线 |
|------|-----------|------|
| 小灵 | companion（陪伴型） | 活泼女声 |
| 小山 | 自定义形象 | 沉稳男声 |
| 妙音 | ancient-beauty（古装美女） | 温柔女声 |
| 小禅 | 自定义形象 | 成熟男声 |

---

## 二、管理后台增强

- **数据大屏**：集成 Recharts 图表（7 天趋势折线图 + 今日时段柱状图），数据来自 SQLite 真实统计
- **知识库管理**：真实 CRUD + 分页搜索 + 文件上传（docx/xlsx/txt）自动切片向量化入库
- **一键导入示例资料**：解析赛题提供的 Docx 文件，自动导入为知识条目 + ChromaDB 向量化
- **景区管理**（新增）：多景区增删改查，ChromaDB 集合自动创建和同步
- **反馈报告**：对话记录查询 + 👍👎 满意率统计

---

## 三、对话增强

- 流式 SSE 响应，打字机效果逐字显示
- 多轮对话上下文记忆（最近 10 轮历史自动拼入提示词）
- 每条 AI 回复支持 👍👎 满意度评价
- 4 导游角色实时切换（首页选择 + 对话页下拉框）

---

## 四、游客行为数据应用

- 14 万条游客行为数据导入 SQLite
- 推荐 API：按年龄/预算/出游类型推荐高满意度景点
- 洞察 API：灵山相关景点访问量/花费/满意度统计
- 推荐页底部展示「相似游客还喜欢」

---

## 五、导览页（TourPage）全面升级

### 5.1 设计重构（critique → polish → layout → distill → harden → bolder）

**启发式评分：24/40（Acceptable）→ 30/40（Good），+6 分（25%↑）**

| 维度 | 上一轮 | 本轮 | 提升原因 |
|------|-------|------|----------|
| H1 系统状态 | 2 | 3 | 实时时钟 30s 更新、天气重试按钮、表单加载状态 |
| H2 真实匹配 | 3 | **4** | 真图替换占位图、佛教术语精准、SVG 图标系统 |
| H4 一致性 | 3 | 3.5 | 投影全部暖色调、CSS 变量 - -spot 主题色统一 |
| H7 灵活效率 | **1** | **3** | 新增搜索栏、标签筛选、收藏、分组展开/折叠 |
| H8 美学简约 | 4 | 3.5 | 液态玻璃卓越，但 150 字截断丢失核心内容 |
| H10 帮助引导 | **1** | 2.5 | 新增首次使用 3 步引导覆盖层 |

### 5.2 图片资源

- **15 张真实景点照片** 替换全部 Picsum 占位图，存放于 `frontend/public/images/spots/`
- 新增 `ScenicSpot.heroImage` 字段：灵山大佛、九龙灌浴、灵山梵宫 3 个 Hero 景点使用专用大卡片图
- 打卡图片改用首字彩色占位符（`hexToRgba` 主题色底色 + 衬线体首字）

### 5.3 图标系统

- **10 个 emoji 图标全部替换为自定义 SVG 图标**，灵感源于佛教建筑与法器：
  | 图标 | 形态 | 用于 |
  |------|------|------|
  | 佛 | 立像剪影 + 莲座 | 灵山大佛 |
  | 莲 | 八瓣莲花 + 水滴 | 九龙灌浴 |
  | 殿 | 人字顶宝殿 | 灵山梵宫 |
  | 塔 | 舍利塔 + 塔刹 | 五印坛城、曼飞龙塔、拈花湾 |
  | 门 | 牌坊门 + 横梁 | 祥符禅寺、灵山大照壁 |
  | 桥 | 双拱 + 竖柱 | 五明桥·佛足坛 |
  | 手 | 佛手掌印 | 佛手广场 |
  | 亭 | 禅亭 + 竖柱 | 灵山精舍（兜底） |

- 统一 1.6px 描边、`strokeLinecap="round"`，Hero 轮播中转半透白适配暗底

### 5.4 搜索、筛选与收藏

- **实时搜索栏**：按景点名称/简介实时过滤，带搜索图标和清除按钮
- **横向标签筛选**：全部 / 建筑 / 演出 / 祈福 / 禅修 / 唐风小镇 / 夜游
- **收藏功能**：卡片右上角星标按钮，localStorage 持久化，收藏筛选单独标签
- **空状态**：无匹配时显示搜索图标 + "未找到匹配的景点" + 清除筛选按钮

### 5.5 卡片分组重构

- 替换单一"查看全部"按钮为 **每组独立展开/折叠 toggle**
- 移动端默认仅展开「核心区」，桌面端全部展开且箭头隐藏
- 分组使用 max-height 过渡实现折叠动画
- 新增 `SPOT_GROUPS` 类型声明：核心区（4 景点）、文化体验（4 景点）、禅修静心（3 景点）

### 5.6 新增景点

- **拈花湾禅意小镇**（第 11 个景点），含完整 description、practicalInfo、5 条 guideTips
- 色值 `#b0845a`（暖琥珀），标签：唐风小镇 / 夜游 / 禅修体验

### 5.7 天气与环境

- **实时时钟**：`setInterval` 每 30 秒更新显示，同时重新评估时段色调（晨/午/暮/夜）
- **天气错误重试**：`⁇` → cloud-off SVG 图标 + "天气未知" + 底部背景提升可读性 + 点击重试 `fetchWuxiWeather()`
- **Carousel 暂停**：hover/touch 时暂停自动轮播

### 5.8 首次引导（Onboarding）

- 3 步骤 coach-mark 覆盖层：探索景点 → 聆听讲解 → 发现更多
- localStorage-gated，完成后永不重现
- 暖色遮罩 + 玻璃卡片弹窗，每步有跳过/下一步按钮
- 保留 Hero "入境"诗意标题，通过引导揭示交互

### 5.9 音频与错误处理

- **三级 TTS 降级**：浏览器 SpeechSynthesis → Edge TTS API → fallback voice
- **音频错误 UI**：按钮变为柔红底色 + 警告图标 + "播放失败，点击重试"
- `playIdRef` 计数器防止过期回调
- **Image onError**：降级展示 SpotIcon SVG

### 5.11 详情面板

- **Escape 键关闭**：`keydown` 监听 Close → 面板关闭
- **手风琴渐进展示**：攻略默认展开、评价/打卡默认折叠
- **表单提交错误提示**：红色内联错误横幅 "发布失败，请检查网络后重试"
- **CSS 动画优化**：expo-out 缓动 `cubic-bezier(0.16, 1, 0.3, 1)` 全程统一

### 5.12 评价 & 打卡接入后端数据库

**数据库层**（`backend/app/db/database.py`）：

| 表名 | 字段 |
|------|------|
| `visitor_reviews` | id, spot_id (索引), author, avatar, rating (1-5), text, created_at |
| `visitor_checkins` | id, spot_id (索引), author, image, caption, created_at |

- 首次启动自动建表，种子数据 10 条评价 + 6 条打卡
- 移除前端 50+ 行硬编码假数据

**API 层**（`backend/app/api/admin.py`，6 个新端点）：

| Method | Path | 用途 |
|--------|------|------|
| GET | `/api/admin/reviews?spot_id=` | 按景点获取评价列表 |
| POST | `/api/admin/reviews` | 提交新评价 |
| DELETE | `/api/admin/reviews/{id}` | 管理后台删除 |
| GET | `/api/admin/checkins?spot_id=` | 按景点获取打卡列表 |
| POST | `/api/admin/checkins` | 提交新打卡 |
| DELETE | `/api/admin/checkins/{id}` | 管理后台删除 |

**前端**（`frontend/src/services/api.ts` + `TourPage.tsx`）：
- 打开详情面板时并行 fetch 评价 + 打卡
- 提交改为异步 POST，支持 loading/error 状态
- 空状态："暂无评价，成为第一个留下足迹的人吧"

### 5.13 CSS 清理

| 问题 | 修复 |
|------|------|
| 重复 `@keyframes zenTextIn` | 删除第二个定义（~20 行死代码） |
| 重复 `.page-atmo--snow .page-snow` | 删除第一个声明 |
| `border-radius: 1px` | → `2px`（下划线装饰） |
| `#d4a94b` 色值 ×2 | → `var(--gold, #c8963e)` |
| `text-shadow` 不可见 | 暗影→亮影 `rgba(61,54,48,0.18)` |
| 未使用 state | 移除 `expanded`（被 per-group toggle 替代） |
| 6 处冷灰/纯黑投影 | → 暖调古铜 `rgba(155,132,101,...)` |
| `prefers-reduced-motion` | Canvas 雨/雪效果全部降级 |

---

## 六、基础设施

- Docker Compose 环境变量统一（DeepSeek API Key/Base/Model 三处一致）
- Python 脚本自动加载 .env 文件
- PWA 支持（Service Worker 离线缓存 + manifest.json）
- HTTPS 本地开发环境（麦克风权限需要）
