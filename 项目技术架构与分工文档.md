# 项目技术架构与分工文档

> **项目名称：** AI数字人智能导游系统  
> **赛题：** 中国软件杯 2026 A5  
> **更新日期：** 2026/06/17

---

## 一、技术架构

### 1.1 整体架构概览

```
┌─────────────────────────────────────────────────────────────────────┐
│                        游客交互端 (Frontend)                          │
│  React 18 + TypeScript + Vite                                       │
│  ┌──────────┬────────────┬────────────┬────────────┐               │
│  │ 首页     │ AI对话     │ 导览地图   │ 路线推荐   │               │
│  │ 景区展示 │ 语音/文字  │ SVG手绘    │ 3条路线    │               │
│  │ 导游选择 │ 流式输出   │ 景点标记   │ 展开式卡片│               │
│  └──────────┴────────────┴────────────┴────────────┘               │
├─────────────────────────────────────────────────────────────────────┤
│                        管理后台 (Frontend)                            │
│  Ant Design + Ant Design Icons                                     │
│  ┌──────────┬────────────┬────────────┬────────────┐               │
│  │ 数据大屏 │ 知识库管理  │ 数字人管理  │ 反馈报告   │               │
│  │ 统计卡片 │ CRUD表格   │ 外观/语音  │ 情感趋势   │               │
│  │ 图表占位 │ Mock数据   │ 配置选项   │ 服务建议   │               │
│  └──────────┴────────────┴────────────┴────────────┘               │
├─────────────────────────────────────────────────────────────────────┤
│                       API 网关 (FastAPI)                              │
│  /api/chat  /api/voice  /api/admin  /api/upload  /api/health       │
├─────────────────────────────────────────────────────────────────────┤
│                         AI 核心层                                     │
│  ┌────────────┬──────────────┬────────────┬──────────────┐         │
│  │ LLM       │ ASR          │ TTS        │ 数字人驱动    │         │
│  │ DeepSeek   │ faster-whisper│ edge-tts  │ SVG渲染+动画  │         │
│  │ 同步/流式  │ 中文语音识别  │ 3种声线    │ 5种情绪表达  │         │
│  └────────────┴──────────────┴────────────┴──────────────┘         │
│  ┌──────────────────────────────────────────────────────┐           │
│  │               RAG 检索增强生成                         │           │
│  │  LangChain文本切片 + sentence-transformers向量化       │           │
│  │  + ChromaDB向量检索 + 增强提示词构建                    │           │
│  └──────────────────────────────────────────────────────┘           │
├─────────────────────────────────────────────────────────────────────┤
│                         数据层                                        │
│  ChromaDB(向量知识库,62片段)  SQLite(业务数据,SQLAlchemy ORM)        │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈明细

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **前端框架** | React + TypeScript | 18.3 / 5.6 | SPA 开发 |
| **构建工具** | Vite | 6.0 | 前端打包与热更新 |
| **UI 组件库** | Ant Design | 5.22 | 管理后台组件 |
| **图标库** | @ant-design/icons + lucide-react | - | 图标资源 |
| **状态管理** | Zustand | 5.0 | 全局状态 |
| **HTTP 客户端** | Axios | 1.7 | API 调用封装 |
| **路由** | react-router-dom | 6.28 | 页面路由 |
| **语音 API** | Web Speech API | 浏览器原生 | 前端 TTS 降级 |
| **后端框架** | FastAPI | 0.115 | 异步 API 服务 |
| **ASGI 服务器** | Uvicorn | 0.34 | Python ASGI 运行 |
| **LLM** | DeepSeek API | v3 | 智能问答生成 |
| **ASR** | faster-whisper | 1.0 | 语音转文字 |
| **TTS** | edge-tts | 6.1 | 文字转语音 |
| **向量数据库** | ChromaDB | 0.5 | 知识库向量存储 |
| **嵌入模型** | sentence-transformers (text2vec-base-chinese) | - | 文本向量化 |
| **RAG 框架** | LangChain | 0.3 | 文本切片与文档加载 |
| **数据库** | SQLite + SQLAlchemy | 2.0 | 业务数据存储 |
| **文档解析** | python-docx / openpyxl | - | 解析 docx/xlsx |
| **HTTP 客户端** | httpx | 0.28 | Python 异步 HTTP |
| **部署** | Docker + Docker Compose | - | 容器化部署 |

### 1.3 项目文件结构

```
scenic-digital-human-agent/
│
├── backend/                          # Python 后端
│   ├── main.py                       # 启动入口 (uvicorn)
│   ├── requirements.txt              # Python 依赖 (19个包)
│   ├── Dockerfile                    # 后端容器化
│   ├── .env.example                  # API Key 配置模板
│   ├── chroma_db/                    # 预构建向量知识库 (62片段)
│   ├── scripts/
│   │   ├── build_knowledge_base.py   # 知识库构建脚本
│   │   └── test_full_pipeline.py     # 端到端测试脚本
│   └── app/
│       ├── app.py                    # FastAPI 应用创建 + 路由注册
│       ├── config.py                 # 配置管理 (Settings 类)
│       ├── api/                      # API 路由层
│       │   ├── chat.py               # 对话接口 (send/stream)
│       │   ├── voice.py              # 语音接口 (asr/tts)
│       │   ├── admin.py              # 管理后台接口 (桩代码)
│       │   └── upload.py             # 文件上传接口 (桩代码)
│       ├── core/                     # AI 核心模块
│       │   ├── llm.py                # DeepSeek 调用 (同步/流式)
│       │   ├── rag.py                # RAG 检索增强生成
│       │   ├── asr.py                # faster-whisper 语音识别
│       │   ├── tts.py                # edge-tts 语音合成
│       │   └── digital_human.py      # 数字人驱动 (TODO)
│       ├── db/
│       │   └── database.py           # SQLAlchemy 数据模型
│       └── models/                   # (预留) Pydantic 数据模型
│
├── frontend/                         # React 前端
│   ├── package.json                  # 19个依赖包
│   ├── vite.config.ts                # Vite 配置 (代理 /api)
│   ├── Dockerfile                    # 前端容器化
│   ├── index.html                    # 入口 HTML
│   └── src/
│       ├── main.tsx                  # React 入口
│       ├── App.tsx                   # 路由配置 (8个页面)
│       ├── index.css                 # 全局样式 (暖色旅游主题)
│       ├── components/
│       │   └── DigitalHuman.tsx      # SVG 数字人组件 (282行)
│       ├── config/
│       │   └── personas.ts           # 3个AI导游角色配置
│       ├── services/
│       │   └── api.ts                # 全部 API 调用封装
│       ├── utils/
│       │   └── voice.ts              # 语音工具 (浏览器 TTS)
│       └── pages/
│           ├── tourist/              # 游客端 (4页面)
│           │   ├── Layout.tsx        # 底部导航栏
│           │   ├── HomePage.tsx      # 首页: 景区+导游选择
│           │   ├── ChatPage.tsx      # 对话页: 数字人+语音/文字
│           │   ├── TourPage.tsx      # 导览地图: SVG景点标记
│           │   └── RecommendPage.tsx # 路线推荐: 3条路线
│           └── admin/                # 管理后台 (4页面)
│               ├── Layout.tsx        # Ant Design 侧边栏布局
│               ├── Dashboard.tsx     # 数据大屏
│               ├── KnowledgeBase.tsx # 知识库管理 CRUD
│               ├── DigitalHuman.tsx  # 数字人管理配置
│               └── ReportPage.tsx    # 游客反馈报告
│
├── docker-compose.yml                # 一键部署 (后端+前端)
├── DEVELOPMENT.md                    # 开发指南
├── plan.md                           # 原始设计文档
├── 记录.md                           # 迭代记录
└── 示范景区公开资料包/                 # 竞赛提供原始数据
    ├── 灵山胜境：历史、文化、景点特色与个性化游览指南.docx
    ├── 灵山胜境 景点结构化数据集.docx
    └── 景点景区旅游数据行为分析数据.xlsx (140K条记录)
```

---

## 二、已完成功能

### 2.1 后端 (AI 核心链路)

| 功能 | 状态 | 说明 |
|------|------|------|
| **RAG 知识库构建** | ✅ 已完成 | 从 docx 文档提取文本 → RecursiveCharacterTextSplitter 切片 (chunk_size=500, overlap=50) → sentence-transformers (text2vec-base-chinese) 向量化 → ChromaDB 存储，共 62 个向量片段 |
| **RAG 检索问答** | ✅ 已完成 | 用户问题 → ChromaDB 检索 top_k=5 → 构建带知识上下文的提示词 → 调 LLM 生成回答 |
| **DeepSeek LLM 集成** | ✅ 已完成 | 支持同步 (`chat`) 和 SSE 流式 (`chat_stream`) 两种模式，含系统角色设定 "专业AI导游" |
| **faster-whisper ASR** | ✅ 已完成 | CPU (int8) 推理，支持中文语音识别，返回文本+置信度 |
| **edge-tts 语音合成** | ✅ 已完成 | 免费高自然度中文 TTS，默认声线 zh-CN-XiaoxiaoNeural，MP3 输出 |
| **对话 API** | ✅ 已完成 | `POST /api/chat/send` 同步对话 + `POST /api/chat/stream` SSE 流式 |
| **语音 API** | ✅ 已完成 | `POST /api/voice/asr` 语音转文本 + `POST /api/voice/tts` 文本转语音 |
| **管理后台 API** | ⚠️ 桩代码 | 知识库 CRUD / 数字人配置 / 数据大屏 / 报表 → 仅返回 Mock 数据 |
| **文件上传** | ⚠️ 桩代码 | 接收文件但不执行切片和入库 |

### 2.2 前端游客端

| 功能 | 状态 | 说明 |
|------|------|------|
| **首页（景区展示 + 导游选择）** | ✅ 已完成 | 灵山胜境封面 → 3个 AI 导游选择（小灵/慧觉/妙音）→ 5个快捷提问 → 进入对话/导览入口 |
| **AI 对话页** | ✅ 已完成 | 文字/语音输入 → 发送消息 → 接收回复 → 历史记录 localStorage 持久化 → 清除记录 |
| **语音输入** | ✅ 已完成 | MediaRecorder 录音 → 调用 `/api/voice/asr` 识别 → 自动发送 |
| **语音播报（三级降级）** | ✅ 已完成 | ① 浏览器 SpeechSynthesis(edge声线匹配) → ② edge-tts API(后端) → ③ 浏览器降级中文语音 |
| **AI 回复播报按钮** | ✅ 已完成 | 每条 AI 回复可点击播报/停止，说话时数字人面部动画联动 |
| **情绪检测** | ✅ 已完成 | 基于正则匹配回复文字 → 5种情绪 (happy/thinking/surprised/sad/neutral) → 数字人表情联动 |
| **导览 SVG 地图** | ✅ 已完成 | 10 个景点标记 + 3 条路线连线 + 点击讲解 + 数字人播报 + 实用信息标签 |
| **路线推荐** | ✅ 已完成 | 3 条路线卡片（历史文化6h/自然风光5h/亲子4h）→ 展开时间轴详情 → 一键导览 |
| **SVG 数字人组件** | ✅ 已完成 | 3 种发型(短/中/长) + 肤色/瞳色/眼镜可选 + 5 种表情 + 说话动画 + 听音脉冲动画 + 思考气泡 |

### 2.3 前端管理后台

| 功能 | 状态 | 说明 |
|------|------|------|
| **数据大屏** | ⚠️ Mock 数据 | 4 个统计卡片 + 热门问答排行表格 + 2 个图表区(占位符) |
| **知识库管理** | ⚠️ Mock 数据 | 表格展示 + 新增/编辑/删除弹窗 + 批量导入按钮 |
| **数字人管理** | ⚠️ Mock 数据 | 当前数字人配置(外观/语音/语速) + 备选数字人启用 |
| **反馈报告** | ⚠️ Mock 数据 | 情感趋势表格 + 3 个汇总卡片 + 服务建议列表 |

### 2.4 基础设施

| 功能 | 状态 |
|------|------|
| Docker 后端容器化 | ✅ 已完成 |
| Docker 前端容器化 | ✅ 已完成 |
| Docker Compose 编排 | ✅ 已完成 |
| 开发指南文档 (DEVELOPMENT.md) | ✅ 已完成 |
| 设计规划文档 (plan.md) | ✅ 已完成 |

---

## 三、待完成任务

### 3.1 核心功能补全（优先级最高）

| 编号 | 任务 | 现状 | 预计工作量 | 重要性 |
|------|------|------|-----------|--------|
| P-01 | **文件上传→RAG 全链路打通** | 上传后不执行切片和入库 | 1天 | 🔴 关键 |
| P-02 | **前端接入流式 API** | ChatPage 调用了同步 `/send`，未使用 `/stream` | 1天 | 🔴 关键 |
| P-03 | **会话上下文管理** | 无真实多轮对话上下文传入 LLM | 1天 | 🔴 关键 |
| P-04 | **知识库 CRUD 对接真实数据库** | API 和前端均使用 Mock 数据 | 2天 | 🔴 关键 |
| P-05 | **支持多景区** | 目前仅硬编码"灵山胜境" | 1天 | 🟡 重要 |

### 3.2 功能深化（中等优先级）

| 编号 | 任务 | 说明 | 预计工作量 | 重要性 |
|------|------|------|-----------|--------|
| P-06 | **数据大屏集成真实图表(ECharts/Recharts)** | 接入 SQLite 数据，替换占位符 | 2天 | 🟡 重要 |
| P-07 | **数字人口型同步** | `digital_human.py` 目前是空壳 | 2天 | 🟡 重要 |
| P-08 | **游客行为分析+个性化推荐** | 基于 140K 条游客数据的推荐算法 | 3天 | 🟢 加分 |
| P-09 | **PWA 打包** | Service Worker + manifest 移动端安装 | 1天 | 🟢 加分 |
| P-10 | **导览地图交互增强** | 手势缩放/拖动 | 1天 | 🟢 加分 |

### 3.3 交付准备

| 编号 | 任务 | 说明 | 预计工作量 |
|------|------|------|-----------|
| P-11 | **产品部署和使用手册** | 安装步骤 + 环境要求 + 常见问题 | 1天 |
| P-12 | **产品总体设计文档** | 架构 + API + 数据库设计 | 2天 |
| P-13 | **产品方案介绍 PPT** | 截图 + 架构图 + 创新点 | 1天 |
| P-14 | **演示视频录制** | ≤7 分钟，展示完整闭环 | 2天 |
| P-15 | **全链路调优与测试** | 延迟 < 5 秒，稳定性测试 | 2天 |

---

## 四、三人分工方案

### 分工总览

```
                    ┌──────────────────┐
                    │   成员A (后端AI)  │
                    │  DeepSeek + RAG   │
                    │  ASR + TTS + API  │
                    └────────┬─────────┘
                             │ 提供 API
                             ↓
┌──────────────────┐  ┌──────────────────┐
│   成员B (前端)    │  │   成员C (全栈)    │
│  游客端交互       │  │  管理后台         │
│  数字人渲染       │  │  Docker+文档     │
│  语音流式        │  │  PPT+视频        │
└──────────────────┘  └──────────────────┘
```

---

### 👤 成员A — 后端核心 + AI 链路

**角色定位：** 后端工程师 / AI 算法工程师  
**核心职责：** 打通从"数据输入→知识入库→RAG检索→LLM生成"的完整 AI 链路

#### 具体任务

| 优先级 | 编号 | 任务 | 详细说明 |
|--------|------|------|----------|
| 🔴 P0 | P-01 | **文件上传 RAG 全链路打通** | `upload.py` 接收文件 → python-docx/openpyxl 解析 → RecursiveCharacterTextSplitter 切片 → ChromaDB 向量化入库。支持 docx/xlsx 格式 |
| 🔴 P0 | P-02 | **前端对接流式接口** | 确保 `/api/chat/stream` SSE 接口稳定，ChatPage 通过 EventSource/fetch + ReadableStream 实现打字机效果 |
| 🔴 P0 | P-03 | **会话上下文管理** | session_id → 从 SQLite ChatRecord 加载历史 → 传入 LLM system prompt 实现多轮对话 |
| 🟡 P1 | P-04 | **知识库真实 CRUD API** | Admin 端知识库增删改查接口对接 SQLite，替换 Mock |
| 🟡 P1 | P-05 | **多景区可配置化** | 景区配置从硬编码改为数据库读取，支持新增景区知识库 |
| 🟢 P2 | P-07 | **数字人口型同步** | 实现 phoneme/viseme 生成接口，返回前端用于 SVG 口型动画驱动 |
| 🟢 P2 | - | **LLM 切换/降级** | 支持在 DeepSeek / Qwen 之间切换（docker-compose 已有相关变量） |
| 🟢 P2 | P-15 | **后端性能优化** | 响应时间 < 5s，必要时引入缓存/并发控制 |

#### 涉及文件

```
backend/app/core/llm.py          # DeepSeek 调用增强
backend/app/core/rag.py          # RAG 检索优化
backend/app/core/asr.py          # ASR 稳定性
backend/app/core/tts.py          # TTS 降级策略
backend/app/core/digital_human.py # 口型同步实现
backend/app/api/chat.py          # 流式稳定化 + 上下文
backend/app/api/upload.py        # 文件→RAG 全链路
backend/app/api/admin.py         # 知识库真实 CRUD
backend/app/db/database.py       # 数据模型补充
backend/scripts/*                # 知识库构建脚本更新
```

---

### 👤 成员B — 前端游客端 + 数字人渲染

**角色定位：** 前端工程师 / 交互设计师  
**核心职责：** 打造流畅、自然、有趣的游客交互体验

#### 具体任务

| 优先级 | 编号 | 任务 | 详细说明 |
|--------|------|------|----------|
| 🔴 P0 | P-02 | **流式打字机效果** | 重写 `ChatPage` 中 `sendMessage` 函数，通过 `fetch` 读取 SSE 流，逐 token 显示 AI 回复，添加光标闪烁效果 |
| 🔴 P0 | - | **SVG 数字人动画增强** | 添加眨眼动画（定时触发）、说话时口型微动（配合语音）、头部轻微晃动、呼吸效果 |
| 🟡 P1 | P-09 | **PWA 打包** | 配置 vite-plugin-pwa，注册 Service Worker，添加 manifest.json（应用名/图标/主题色），支持"添加到主屏幕" |
| 🟡 P1 | P-10 | **导览地图交互增强** | 添加手势缩放（pinch zoom）、拖拽平移、当前位置模拟显示 |
| 🟡 P1 | - | **移动端全面适配** | iOS Safari 底部安全区、Android 状态栏、不同屏幕尺寸断点调试、触摸事件优化 |
| 🟢 P2 | - | **对话体验优化** | 快捷回复建议气泡、输入中联想提示、消息加载骨架屏、网络断开重连提示 |
| 🟢 P2 | - | **推荐页面动态化** | 对接推荐算法 API，展示个性化动态路线（非固定3条） |
| 🟢 P2 | - | **快捷键/暗黑模式** | 备选加分项 |

#### 涉及文件

```
frontend/src/pages/tourist/ChatPage.tsx    # 流式+语音+情绪联动
frontend/src/pages/tourist/TourPage.tsx    # 导览图交互增强
frontend/src/pages/tourist/HomePage.tsx    # 首页优化
frontend/src/pages/tourist/RecommendPage.tsx # 推荐动态化
frontend/src/components/DigitalHuman.tsx    # 数字人动画增强
frontend/src/services/api.ts              # 流式 API 封装
frontend/src/config/personas.ts           # 角色配置（可能新增）
frontend/src/utils/voice.ts              # 语音工具优化
frontend/src/index.css                    # 全局样式补充
```

---

### 👤 成员C — 管理后台 + 交付文档

**角色定位：** 全栈工程师 / 文档负责人  
**核心职责：** 管理后台真实化 + 确保项目可交付

#### 具体任务

| 优先级 | 编号 | 任务 | 详细说明 |
|--------|------|------|----------|
| 🔴 P0 | P-04 | **Admin 后台全功能真实化** | 知识库 CRUD（与成员A配合 API 对接）、数字人配置持久化、数据大屏从 SQLite 读取真实统计 |
| 🔴 P0 | P-06 | **数据大屏图表集成** | 安装 ECharts 或 Recharts，接入真实 /api/admin/dashboard 数据，展示日/周服务人次趋势、满意度趋势、热门问答排行 |
| 🔴 P0 | - | **Docker 部署统一调通** | docker-compose 环境变量与后端实际使用的环境变量名统一（当前混用了 `DEEPSEEK_API_KEY` / `LLM_API_KEY`），确保一键部署可用 |
| 🟡 P1 | - | **游客反馈分析模块** | 基于 ChatRecord 数据的情感倾向分析 + 热门话题聚合 + 可视化展示 |
| 🟡 P1 | P-12 | **产品总体设计文档** | 架构图 + API 接口文档（OpenAPI/Swagger导出） + 数据库ER图 + 模块说明 |
| 🟡 P1 | P-11 | **部署使用手册** | 环境要求 + 安装步骤 + 配置说明 + 常见问题 FAQ |
| 🟢 P2 | P-13 | **PPT 制作** | 产品方案介绍：背景→技术方案→功能展示→创新点→展望 |
| 🟢 P2 | P-14 | **演示视频录制** | ≤7 分钟，展示游客端闭环 + 管理后台操作 + 技术亮点 |
| 🟢 P2 | P-15 | **自动化测试** | 核心 API 的 pytest 测试 + 前端 smoke test |

#### 涉及文件

```
frontend/src/pages/admin/Dashboard.tsx      # 图表集成
frontend/src/pages/admin/KnowledgeBase.tsx   # 真实 CRUD 对接
frontend/src/pages/admin/DigitalHuman.tsx    # 真实配置对接
frontend/src/pages/admin/ReportPage.tsx      # 情感分析展示
backend/app/api/admin.py                     # 真实 API（与成员A协同）
docker-compose.yml                           # 环境变量统一
DEVELOPMENT.md                               # 更新开发指南
```

---

### 协作依赖关系

```
成员A (后端AI)                      成员B (前端)                   成员C (全栈+文档)
─────────────                      ──────────                    ────────────────
[P-01] 文件→RAG                     [P-02] 流式打字机              [P-04] Admin真实CRUD
        ↓ 提供上传API                  ↓ 对接 /stream              ↑ 依赖A的API
[P-03] 会话上下文                    [P-09] PWA打包               [P-06] 大屏图表
        ↓ 提供session API             ↑ 独立完成                   ↑ 依赖A的dashboard数据
[P-05] 多景区配置                    [P-10] 导览交互              [P-11~14] 文档/PPT/视频
        ↑ 独立完成                     ↑ 独立完成                   ↑ 依赖整体完成度
```

**关键联合协作点：**
1. **P-01 / P-04：** 成员A完成后端文件上传RAG API + 知识库CRUD API → 成员C对接前端管理后台
2. **P-02：** 成员A保证 `/api/chat/stream` 稳定 → 成员B实现前端流式效果
3. **P-03：** 成员A实现会话管理 → 成员B在ChatPage中集成 session_id
4. **P-11~14：** 成员C产出文档/PPT/视频前，需要A和B的功能基本完成

---

### 建议周里程碑

| 周次 | 成员A | 成员B | 成员C |
|------|-------|-------|-------|
| **第1周** | P-01 文件RAG打通 | P-02 流式打字机效果 | P-04 Admin真实CRUD |
| | P-02 流式接口稳定 | 数字人动画增强 | P-06 Docker调通 |
| **第2周** | P-03 会话上下文管理 | P-09 PWA打包 | P-06 大屏图表集成 |
| | P-05 多景区配置 | P-10 导览图优化 | 情感分析模块 |
| **第3周** | P-07 口型同步 | 推荐前端动态化 | P-12 设计文档 |
| | LLM切换/降级 | 移动端适配 | P-11 部署手册 |
| **第4周** | P-15 联调+压测 | Bug修复+体验打磨 | P-13 PPT + P-14 视频 |
| | 性能优化(<5s) | 最后润色 | 最终交付整合 |

---

## 附录

### A. 多模态数据流

```
用户语音输入
    ↓
🎤 MediaRecorder 录音 → audio/webm blob
    ↓
[ASR API] POST /api/voice/asr → faster-whisper 识别
    ↓
用户文字输入（或 ASR 结果文本）
    ↓
[RAG 检索] ChromaDB 查询 top_k=5 → 相关文档片段
    ↓
[Prompt 构建] 系统角色 + 知识上下文 + 用户问题
    ↓
[LLM 推理] DeepSeek API → 回答文本（同步 或 SSE 流式）
    ↓
[前端展示] 流式逐 token 显示 ← 或 ← 完整回复显示
    ↓
[TTS 播报] ① 浏览器 SpeechSynthesis → ② edge-tts API → ③ 降级中文语音
    ↓
[数字人联动] 情绪检测 → 表情切换 + 说话口型动画
```

### B. API 接口清单

| 方法 | 路径 | 功能 | 状态 |
|------|------|------|------|
| GET | /api/health | 健康检查 | ✅ |
| POST | /api/chat/send | 同步对话 | ✅ |
| POST | /api/chat/stream | 流式对话(SSE) | ✅ |
| POST | /api/voice/asr | 语音识别(上传音频) | ✅ |
| POST | /api/voice/tts | 语音合成(返回MP3) | ✅ |
| GET | /api/admin/knowledge | 知识库列表 | ⚠️ Mock |
| POST | /api/admin/knowledge | 新增知识条目 | ⚠️ Mock |
| PUT | /api/admin/knowledge/{id} | 更新知识条目 | ⚠️ Mock |
| DELETE | /api/admin/knowledge/{id} | 删除知识条目 | ⚠️ Mock |
| GET | /api/admin/digital-humans | 数字人列表 | ⚠️ Mock |
| PUT | /api/admin/digital-humans/{id} | 更新数字人配置 | ⚠️ Mock |
| GET | /api/admin/dashboard | 数据大屏 | ⚠️ Mock |
| GET | /api/admin/reports | 反馈报告 | ⚠️ Mock |
| POST | /api/upload/document | 上传文档 | ⚠️ 桩代码 |

### C. 数据库模型 (SQLite)

```
ChatRecord:    id | session_id | scenic_spot | user_input | ai_reply | created_at
KnowledgeDoc:  id | title | content | category | scenic_spot | created_at | updated_at
DigitalHumanConfig: id | name | scenic_spot | avatar | voice | model_config(JSON) | created_at
```
